pyyaml>=6.0
anthropic>=0.40
faster-whisper>=1.0
sounddevice>=0.4
webrtcvad-wheels>=2.0
numpy>=1.24
PyQt6>=6.6
piper-tts>=1.2

# Linux only
evdev>=1.6; sys_platform == "linux"

# macOS / Windows hotkey
pynput>=1.7; sys_platform != "linux"

# Optional — cloud TTS (ElevenLabs)
# elevenlabs>=1.0

# Optional — speaker verification (filters out non-user voices in always-on)
# resemblyzer>=0.1
